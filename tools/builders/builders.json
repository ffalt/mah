{
	"builders": {
		"application": {
			"implementation": "./application.mjs",
			"schema": "./application-schema.json",
			"description": "Application builder wrapper that injects define values and an index.html transformer."
		},
		"dev-server": {
			"implementation": "./dev-server.mjs",
			"schema": "./dev-server-schema.json",
			"description": "Dev-server builder wrapper that forwards the same extensions (index.html transformer) to the underlying @angular/build dev server."
		}
	}
}

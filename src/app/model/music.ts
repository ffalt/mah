export class Music {
	enabled: boolean = false;

	play() {
		// if (!this.enabled) {
		// 	return;
		// }
	}

	pause() {
		// if (!this.enabled) {
		// 	return;
		// }
	}

	toggle() {
		// if (!this.settings.music) {
		// 	this.stop();
		// } else {
		// 	this.play();
		// }
		// toggleMusic(): void {
		// 	this.music.toggle();
		// }
	}
}
